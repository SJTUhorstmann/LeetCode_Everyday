问题描述:给定具有重复项的二叉搜索树（BST），找到给定BST中的所有模式（最常出现的元素）。

假设BST定义如下：

节点的左子树仅包含键小于或等于节点键的节点。
节点的右子树仅包含键大于或等于节点键的节点。
左右子树也必须是二叉搜索树。
 

例如：
鉴于BST [1，null，2,2]，

   1
    \
     2
    /
   2
 

返回[2]。

注意：如果树有多个模式，您可以按任何顺序返回它们