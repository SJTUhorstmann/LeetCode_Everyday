题目描述：使用insert，search和startsWith方法实现trie。

例：

Trie trie = new Trie（）;

trie.insert（ “苹果”）;
trie.search（ “苹果”）; //返回true
trie.search（ “应用程序”）; //返回false
trie.startsWith（ “应用程序”）; //返回true
trie.insert（ “应用程序”）;
trie.search（ “应用程序”）; //返回true

注意：

您可以假设所有输入都由小写字母a-z组成。
所有输入都保证是非空字符串。